# notifier (개인화 알림 발송) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `notifier.send` 스텁을 실구현으로 — setting_info/member_info로 발송여부·수신번호·템플릿을 결정하고 제공사 추상화로 발송. 실 발송(제공사 계정)은 연기.

**Architecture:** `send(shop_key, channel, count, success, *, repo, provider) -> bool`. DB는 `NotifierRepository`(Protocol, 실제 SupabaseNotifierRepository), 발송은 `NotificationProvider`(Protocol, HTTP 골격). 블로킹 호출은 `asyncio.to_thread` 오프로드, 모든 실패는 예외 전파 없이 False. 오프라인은 fake repo/provider로 결정적 테스트.

**Tech Stack:** Python 3.13, supabase-py 2.30.1, httpx, pytest, pytest-asyncio.

설계서: `docs/superpowers/specs/2026-06-03-notifier-design.md`
브랜치: `feature/notifier` (이미 생성됨)

**검증 명령 전제:** 모든 pytest/python은 `backend\.venv\Scripts\python.exe`로, 저장소 루트 `C:\ggotAI\ggotAIorder`에서 실행.

---

## File Structure

| 파일 | 책임 | 유형 |
| --- | --- | --- |
| `backend/src/ggotaiorder/notifier/repository.py` | `NotificationSettings` + `NotifierRepository`(Protocol) + `SupabaseNotifierRepository` | 신규 |
| `backend/src/ggotaiorder/notifier/provider.py` | `NotificationProvider`(Protocol) + `HttpNotificationProvider`(골격) | 신규 |
| `backend/src/ggotaiorder/notifier/sms_sender.py` | `render_template`(유지) + `send()` 오케스트레이션 | 수정 |
| `backend/tests/test_notifier.py` | send() 오프라인 결정적 테스트 | 신규 |
| `backend/README.md` | notifier 라이브 구동 체크리스트 | 수정 |

---

### Task 1: notifier/repository.py — 설정 조회

**Files:** Create `backend/src/ggotaiorder/notifier/repository.py`

`SupabaseNotifierRepository`는 실 DB(통합 영역) — 단위테스트하지 않는다. Protocol 계약 고정 + import 확인. send 단위테스트(Task 3)는 fake로 한다.

- [ ] **Step 1: 구현 작성** — `backend/src/ggotaiorder/notifier/repository.py`:
```python
"""알림 발송 설정 조회: setting_info + member_info(폴백 번호)."""

from __future__ import annotations

import logging
from dataclasses import dataclass
from typing import Optional, Protocol

from ggotaiorder.core.supabase_client import get_client

logger = logging.getLogger(__name__)


@dataclass
class NotificationSettings:
    use_notification: str
    notification_phone_number: Optional[str]
    rpa_success_message: str
    rpa_fail_message: str
    fallback_mobile: Optional[str]


class NotifierRepository(Protocol):
    """알림 발송이 필요로 하는 설정 조회 계약."""

    def get_settings(self, shop_key: int) -> Optional[NotificationSettings]: ...


class SupabaseNotifierRepository:
    """Supabase 기반 NotifierRepository 구현."""

    def get_settings(self, shop_key: int) -> Optional[NotificationSettings]:
        client = get_client()
        setting = (
            client.table("setting_info")
            .select(
                "use_notification, notification_phone_number, "
                "rpa_success_message, rpa_fail_message"
            )
            .eq("shop_key", shop_key)
            .limit(1)
            .execute()
        )
        if not setting.data:
            return None
        row = setting.data[0]

        member = (
            client.table("member_info")
            .select("mobile_number")
            .eq("id", shop_key)
            .limit(1)
            .execute()
        )
        fallback_mobile = member.data[0]["mobile_number"] if member.data else None

        return NotificationSettings(
            use_notification=row.get("use_notification") or "N",
            notification_phone_number=row.get("notification_phone_number"),
            rpa_success_message=row.get("rpa_success_message") or "",
            rpa_fail_message=row.get("rpa_fail_message") or "",
            fallback_mobile=fallback_mobile,
        )
```

- [ ] **Step 2: import 확인** — Run:
```powershell
backend\.venv\Scripts\python.exe -c "from ggotaiorder.notifier.repository import NotificationSettings, NotifierRepository, SupabaseNotifierRepository; print('ok', NotificationSettings('Y', None, 's', 'f', '010').use_notification)"
```
Expected: `ok Y` (네트워크 호출 없음).

- [ ] **Step 3: Commit**
```bash
git add backend/src/ggotaiorder/notifier/repository.py
git commit -m "feat: notifier 설정 조회 repository(setting_info+member_info) 추가"
```

---

### Task 2: notifier/provider.py — 발송 제공사 추상화

**Files:** Create `backend/src/ggotaiorder/notifier/provider.py`

`HttpNotificationProvider`는 라이브 영역(제공사 계정 필요) — 단위테스트하지 않는다. send 테스트는 fake provider로 한다.

- [ ] **Step 1: 구현 작성** — `backend/src/ggotaiorder/notifier/provider.py`:
```python
"""알림 발송 제공사 추상화.

NotificationProvider(Protocol)로 계약을 고정하고, 실제 발송은 제공사별 구현이 담당한다.
HttpNotificationProvider 는 골격이며 실 발송에는 제공사 계정·승인 템플릿이 필요하다(라이브 체크리스트).
"""

from __future__ import annotations

import logging
import os
from typing import Protocol

import httpx

logger = logging.getLogger(__name__)


class NotificationProvider(Protocol):
    """단일 메시지 발송 계약."""

    def send_message(self, to: str, text: str) -> None: ...


class HttpNotificationProvider:
    """범용 HTTP 메시징 제공사 골격 (env 기반).

    NOTIFY_API_URL/NOTIFY_API_KEY 로 설정. 실제 페이로드 규격은 제공사에 맞춰
    완성해야 한다(라이브 체크리스트). 미설정 시 RuntimeError.
    """

    def send_message(self, to: str, text: str) -> None:
        api_url = os.getenv("NOTIFY_API_URL")
        api_key = os.getenv("NOTIFY_API_KEY")
        if not api_url or not api_key:
            raise RuntimeError("NOTIFY_API_URL/NOTIFY_API_KEY 미설정 — 발송 불가")
        resp = httpx.post(
            api_url,
            headers={"Authorization": f"Bearer {api_key}"},
            json={"to": to, "text": text},
            timeout=10.0,
        )
        resp.raise_for_status()
```

- [ ] **Step 2: import 확인** — Run:
```powershell
backend\.venv\Scripts\python.exe -c "from ggotaiorder.notifier.provider import NotificationProvider, HttpNotificationProvider; print('ok')"
```
Expected: `ok`.

- [ ] **Step 3: Commit**
```bash
git add backend/src/ggotaiorder/notifier/provider.py
git commit -m "feat: notifier 발송 제공사 추상화(NotificationProvider+HTTP 골격) 추가"
```

---

### Task 3: notifier/sms_sender.py — send() 오케스트레이션 (TDD)

**Files:** Modify `backend/src/ggotaiorder/notifier/sms_sender.py`; create `backend/tests/test_notifier.py`

기존 `sms_sender.py`는 `render_template`(유지)와 stub `send(channel, count, success)`를 가진다. `render_template`는 그대로 두고 `send`를 재작성한다.

- [ ] **Step 1: 실패 테스트 작성** — `backend/tests/test_notifier.py`:
```python
from ggotaiorder.notifier.repository import NotificationSettings
from ggotaiorder.notifier.sms_sender import render_template, send


class FakeRepo:
    def __init__(self, settings):
        self._settings = settings

    def get_settings(self, shop_key):
        self.last_shop_key = shop_key
        return self._settings


class FakeProvider:
    def __init__(self, raises=False):
        self.sent = []
        self._raises = raises

    def send_message(self, to, text):
        if self._raises:
            raise RuntimeError("provider down")
        self.sent.append((to, text))


def _settings(**kw):
    base = dict(
        use_notification="Y",
        notification_phone_number="010-1111-2222",
        rpa_success_message="{channel} 주문 {count}건 입력 완료",
        rpa_fail_message="[경고] {channel} 주문 입력 실패",
        fallback_mobile="010-9999-0000",
    )
    base.update(kw)
    return NotificationSettings(**base)


def test_render_template_substitutes():
    assert render_template("{channel} {count}건", "인터라넷", 3) == "인터라넷 3건"


async def test_disabled_skips_and_returns_false():
    repo = FakeRepo(_settings(use_notification="N"))
    provider = FakeProvider()
    result = await send(2, "핸드폰", 1, True, repo=repo, provider=provider)
    assert result is False
    assert provider.sent == []


async def test_success_sends_to_notification_number():
    repo = FakeRepo(_settings())
    provider = FakeProvider()
    result = await send(2, "가게전화", 4, True, repo=repo, provider=provider)
    assert result is True
    assert provider.sent == [("010-1111-2222", "가게전화 주문 4건 입력 완료")]


async def test_fallback_to_mobile_when_no_notification_number():
    repo = FakeRepo(_settings(notification_phone_number=None))
    provider = FakeProvider()
    result = await send(2, "쇼핑몰", 2, True, repo=repo, provider=provider)
    assert result is True
    assert provider.sent[0][0] == "010-9999-0000"


async def test_failure_uses_fail_template():
    repo = FakeRepo(_settings())
    provider = FakeProvider()
    result = await send(2, "인터라넷", 1, False, repo=repo, provider=provider)
    assert result is True
    assert provider.sent == [("010-1111-2222", "[경고] 인터라넷 주문 입력 실패")]


async def test_no_recipient_returns_false():
    repo = FakeRepo(_settings(notification_phone_number=None, fallback_mobile=None))
    provider = FakeProvider()
    result = await send(2, "핸드폰", 1, True, repo=repo, provider=provider)
    assert result is False
    assert provider.sent == []


async def test_settings_none_returns_false():
    repo = FakeRepo(None)
    provider = FakeProvider()
    result = await send(2, "핸드폰", 1, True, repo=repo, provider=provider)
    assert result is False


async def test_provider_exception_returns_false():
    repo = FakeRepo(_settings())
    provider = FakeProvider(raises=True)
    result = await send(2, "핸드폰", 1, True, repo=repo, provider=provider)
    assert result is False
```

- [ ] **Step 2: 실패 확인** — Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_notifier.py -v`
Expected: FAIL — 현재 `send(channel, count, success)` 시그니처와 불일치(TypeError) 및 동작 미구현.

- [ ] **Step 3: sms_sender.py 재작성** — `backend/src/ggotaiorder/notifier/sms_sender.py` 전체 교체:
```python
"""개인화 알림 발송.

PRD 6-6: setting_info.use_notification 확인 → 수신번호 결정
(notification_phone_number ?? member_info.mobile_number) → 템플릿
({channel}/{count} 치환) → 제공사 추상화로 발송. 결과는 로그로 기록.
"""

from __future__ import annotations

import asyncio
import logging

from ggotaiorder.notifier.provider import HttpNotificationProvider, NotificationProvider
from ggotaiorder.notifier.repository import NotifierRepository, SupabaseNotifierRepository

logger = logging.getLogger(__name__)


def render_template(template: str, channel: str, count: int) -> str:
    """템플릿의 {channel}/{count} 변수를 실제 값으로 치환한다."""
    return template.replace("{channel}", channel).replace("{count}", str(count))


def _mask(phone: str) -> str:
    """로그용 전화번호 마스킹(뒤 4자리만 노출)."""
    digits = "".join(ch for ch in phone if ch.isdigit())
    return f"***{digits[-4:]}" if len(digits) >= 4 else "***"


async def send(
    shop_key: int,
    channel: str,
    count: int,
    success: bool,
    *,
    repo: NotifierRepository | None = None,
    provider: NotificationProvider | None = None,
) -> bool:
    """RPA 결과 알림을 발송한다. 실제 발송 시 True, 스킵/실패 시 False."""
    repo = repo or SupabaseNotifierRepository()
    provider = provider or HttpNotificationProvider()

    settings = await asyncio.to_thread(repo.get_settings, shop_key)
    if settings is None:
        logger.warning("알림 설정 없음 — 발송 스킵 shop_key=%s", shop_key)
        return False

    if settings.use_notification != "Y":
        logger.info("알림 비활성(use_notification=N) — 스킵 shop_key=%s", shop_key)
        return False

    recipient = settings.notification_phone_number or settings.fallback_mobile
    if not recipient:
        logger.warning("수신번호 없음 — 발송 스킵 shop_key=%s", shop_key)
        return False

    template = settings.rpa_success_message if success else settings.rpa_fail_message
    text = render_template(template, channel, count)

    try:
        await asyncio.to_thread(provider.send_message, recipient, text)
    except Exception:
        logger.exception("알림 발송 실패 shop_key=%s to=%s", shop_key, _mask(recipient))
        return False

    logger.info(
        "알림 발송 성공 shop_key=%s to=%s success=%s", shop_key, _mask(recipient), success
    )
    return True
```

- [ ] **Step 4: 통과 확인** — Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_notifier.py -v`
Expected: 8 passed.

- [ ] **Step 5: 스모크 호환 확인** — Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests/test_smoke.py -v`
Expected: 모두 pass. 단, 기존 `test_smoke.py::test_notifier_template_rendering`이 `from ggotaiorder.notifier.sms_sender import render_template`를 import하므로 정상 동작해야 함(render_template 유지).

- [ ] **Step 6: Commit**
```bash
git add backend/src/ggotaiorder/notifier/sms_sender.py backend/tests/test_notifier.py
git commit -m "feat: notifier send() 실구현(설정·수신번호·템플릿·발송) + 경로별 테스트"
```

---

### Task 4: 전체 검증 + README 체크리스트

**Files:** Modify `backend/README.md`

- [ ] **Step 1: 전체 스위트 실행** — Run: `backend\.venv\Scripts\python.exe -m pytest backend/tests -q`
Expected: 모두 pass + 3 skipped(기존 라이브). 신규 notifier 8 passed 추가. 실제 합계를 보고(대략 61 passed, 3 skipped).

- [ ] **Step 2: README 에 notifier 라이브 체크리스트 추가** — `backend/README.md`의 `## 구조` 섹션 바로 앞에 삽입:
```markdown
## notifier(알림 발송) 라이브 구동 체크리스트

알림 발송 로직은 구현·오프라인 테스트되었으나, 실제 발송에는 다음이 필요합니다:

1. 메시징 제공사 계정/API 키 발급(SMS) + 알림톡 사용 시 발신프로필·승인 템플릿 등록.
2. `.env`에 `NOTIFY_API_URL`/`NOTIFY_API_KEY` 설정, `HttpNotificationProvider`를 제공사 페이로드 규격에 맞춰 완성.
3. `setting_info.use_notification='Y'` + 수신번호(`notification_phone_number` 또는 `member_info.mobile_number`) 설정 후 테스트 발송.
4. (후속) rpa 처리 완료 시 `notifier.send(shop_key, channel, count, success)` 호출 배선.

```

- [ ] **Step 3: Commit**
```bash
git add backend/README.md
git commit -m "docs: notifier 라이브 구동 체크리스트 README 추가"
```

---

## Self-Review 결과

- **Spec 커버리지**: send 시그니처(shop_key 추가, Task3) / 모듈분해 repository·provider·sms_sender(Task1·2·3) / 흐름 use_notification·수신번호 폴백·템플릿·발송·로깅(Task3) / 비동기 to_thread·실패 False(Task3) / 이력 로그+마스킹(Task3 `_mask`) / 제공사 추상화·HTTP 골격(Task2) / 오프라인 테스트 경로별(Task3) / 라이브 체크리스트(Task4) — 설계서 절 모두 매핑.
- **Placeholder 스캔**: repository/provider의 Supabase·HTTP 구현은 통합/라이브 영역(체크리스트), 빈 placeholder 아님. TODO 없음.
- **타입 일관성**: `NotificationSettings(use_notification, notification_phone_number, rpa_success_message, rpa_fail_message, fallback_mobile)`, `NotifierRepository.get_settings(shop_key)->Optional[NotificationSettings]`, `NotificationProvider.send_message(to, text)->None`, `send(shop_key, channel, count, success, *, repo, provider)->bool`, `render_template(template, channel, count)->str` — Task 간·테스트 fake와 시그니처 일치.
- **검증 가능성**: send는 fake repo/provider로 결정적 오프라인. SupabaseNotifierRepository·HttpNotificationProvider는 라이브 영역(import 확인만). 기존 53 회귀 유지(render_template 보존 → 스모크 호환).
- **주의**: `test_smoke.py`의 render_template 테스트가 깨지지 않도록 render_template 시그니처/동작 보존(Task3에서 유지). send가 async가 되어도 스모크는 send를 호출하지 않음(import만) — 영향 없음.

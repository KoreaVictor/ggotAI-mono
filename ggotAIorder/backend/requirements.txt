# --- Web / API ---
fastapi
uvicorn[standard]
python-multipart

# --- Supabase / DB ---
supabase

# --- 환경설정 / 보안 ---
python-dotenv
cryptography

# --- 스케줄러 ---
APScheduler

# --- AI 파이프라인 ---
faster-whisper
google-genai
pydantic

# --- 관리 프로그램 자동입력(RPA) ---
# 프로그램별 의존성은 여기 두지 않는다. 가게가 쓰는 프로그램의 extra 만 설치할 것:
#   pip install -e "backend[flowernt]"   # playwright (FlowerNT 웹)
#   pip install -e "backend[roseweb]"    # uiautomation (RoseWeb 데스크톱)

# --- Windows 서비스 / 트레이 ---
pywin32; sys_platform == "win32"
pystray
Pillow

# --- RPA / 백업 ---
pyperclip
pygetwindow
openpyxl

# --- HTTP (알림 API) ---
httpx

# --- 개발/테스트 ---
pytest
pytest-asyncio
